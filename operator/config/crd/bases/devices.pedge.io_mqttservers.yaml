---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: mqttservers.devices.pedge.io
spec:
  group: devices.pedge.io
  names:
    kind: MQTTServer
    listKind: MQTTServerList
    plural: mqttservers
    singular: mqttserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: MQTTServer is the Schema for the mqttservers API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: MQTTServerSpec defines the desired state of MQTTServer
            properties:
              queue:
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
            required:
            - queue
            type: object
          status:
            description: MQTTServerStatus defines the observed state of MQTTServer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
