---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.13.0
  name: devicesclusters.devices.pedge.io
spec:
  group: devices.pedge.io
  names:
    kind: DevicesCluster
    listKind: DevicesClusterList
    plural: devicesclusters
    singular: devicescluster
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: DevicesCluster is the Schema for the devicesclusters API
        properties:
          apiVersion:
            description: 'APIVersion defines the versioned schema of this representation
              of an object. Servers should convert recognized schemas to the latest
              internal value, and may reject unrecognized values. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources'
            type: string
          kind:
            description: 'Kind is a string value representing the REST resource this
              object represents. Servers may infer this from the endpoint the client
              submits requests to. Cannot be updated. In CamelCase. More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds'
            type: string
          metadata:
            type: object
          spec:
            description: DevicesClusterSpec defines the desired state of DevicesCluster
            properties:
              influxdb:
                properties:
                  namespace:
                    type: string
                  secretReference:
                    description: LocalObjectReference contains enough information
                      to let you locate the referenced object inside the same namespace.
                    properties:
                      name:
                        default: ""
                        description: 'Name of the referent. This field is effectively
                          required, but due to backwards compatibility is allowed
                          to be empty. Instances of this type with an empty value
                          here are almost certainly wrong. TODO: Add other useful
                          fields. apiVersion, kind, uid? More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                          TODO: Drop `kubebuilder:default` when controller-gen doesn''t
                          need it https://github.com/kubernetes-sigs/kubebuilder/issues/3896.'
                        type: string
                    type: object
                    x-kubernetes-map-type: atomic
                type: object
              mqtt:
                properties:
                  sensorsTopic:
                    type: string
                type: object
              persistentVolumeClaimName:
                type: string
            required:
            - mqtt
            type: object
          status:
            description: DevicesClusterStatus defines the observed state of DevicesCluster
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
