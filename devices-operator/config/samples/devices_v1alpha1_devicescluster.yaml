apiVersion: devices.pedge.io/v1alpha1
kind: DeviceCluster
metadata:
  name: devices-cluster
spec:
  mqtt:
    sensorsTopic: sensors
  influxdb:
    # name: influxdb
    namespace: default
    secretReference:
      name: influxdb-auth
